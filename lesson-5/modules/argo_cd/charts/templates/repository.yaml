apiVersion: v1
kind: Secret
metadata:
  name: django-helm-repo
  namespace: argocd
  labels:
    argocd.argoproj.io/secret-type: repository
stringData:
  type: git
  url: { { .Values.djangoHelmRepoURL } }
  name: django-helm-repo
